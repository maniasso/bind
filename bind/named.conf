//
// named.conf
//
// Provided by Red Hat bind package to configure the ISC BIND named(8) DNS
// server as a caching only nameserver (as a localhost DNS resolver only).
//
// See /usr/share/doc/bind*/sample/ for example named configuration files.
//
// See the BIND Administrator's Reference Manual (ARM) for details about the
// configuration located in /usr/share/doc/bind-{version}/Bv9ARM.html

options {
//	listen-on port 53 { 127.0.0.1; };
//	listen-on-v6 port 53 { ::1; };
	directory 	"/bind";
	dump-file 	"/bind/cache_dump.db";
	statistics-file "/bind/named_stats.txt";
	memstatistics-file "/bind/named_mem_stats.txt";
	recursing-file  "/bind/named.recursing";
	secroots-file   "/bind/named.secroots";
	allow-query     { any; };

	/* 
	 - If you are building an AUTHORITATIVE DNS server, do NOT enable recursion.
	 - If you are building a RECURSIVE (caching) DNS server, you need to enable 
	   recursion. 
	 - If your recursive DNS server has a public IP address, you MUST enable access 
	   control to limit queries to your legitimate users. Failing to do so will
	   cause your server to become part of large scale DNS amplification 
	   attacks. Implementing BCP38 within your network would greatly
	   reduce such attack surface 
	*/
	recursion yes;

	forwarders {
                8.8.8.8;
                8.8.4.4;
        };

	dnssec-enable yes;
	dnssec-validation yes;

	/* Path to ISC DLV key */
	bindkeys-file "/bind/named.iscdlv.key";

	managed-keys-directory "/bind";

	pid-file "/bind/named.pid";
	session-keyfile "/bind/session.key";
};

logging {
        channel default_debug {
                file "named.run";
                severity dynamic;
        };
};

#zone "." IN {
#	type hint;
#	file "named.ca";
#};

zone "maniasso.com" IN {
         type master;
         file "maniasso.db";
         allow-update { none; };
};


zone "15.168.192.in-addr.arpa" IN {
          type master;
          file "15.168.192.db";
          allow-update { none; };
};

include "/bind/named.rfc1912.zones";
include "/bind/named.root.key";
